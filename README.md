# Ray-Path-Aware Virtual Point Removal Project Page

## Current structure

```text
ray-path-vpr-project-page-v3/
├── index.html
├── style.css
├── README.md
└── assets/
    └── teaser.jpg
```

## Current status

- Paper: Coming Soon
- Code: Coming Soon
- Video: Coming Soon
- Results: Coming Soon
- `assets/teaser.jpg` is used for both teaser and method overview.

## Later update

When the paper is ready:

1. Put the PDF here:

```text
assets/paper.pdf
```

2. In `index.html`, replace:

```html
<span class="button is-light is-rounded disabled-button">Paper Coming Soon</span>
```

with:

```html
<a class="button is-dark is-rounded" href="assets/paper.pdf">Paper</a>
```

When results are ready, replace the Results section in `index.html` with your figure or table.

## GitHub Pages

Upload everything to your GitHub repository, then enable:

`Settings → Pages → Deploy from a branch → main → /root`
